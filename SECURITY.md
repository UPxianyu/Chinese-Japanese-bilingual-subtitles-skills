# Security Policy

## 报告问题

如发现安全、隐私或版权相关问题，请通过仓库的私密渠道联系维护者，不要在公开 issue 中直接公开敏感细节。

## 敏感信息

- 不要在仓库中提交 API 密钥、令牌、Cookie 或第三方签名密钥。
- `mojigeci` 的签名密钥应通过环境变量 `MOJIGECI_SECRET` 提供。
- 本地生成的 `*.ass`、`*.srt`、`*.json`、音频和视频文件不应提交到仓库。

## 依赖与接口

本仓库脚本会访问外部模型下载源和可选歌词接口。请只使用你信任且有权访问的服务，并遵循其服务条款。
